package kernal:probe@0.1.0;

interface blobs {
    resource blob {
        read: async func() -> stream<u8>;
    }
    granted: async func() -> option<blob>;
}

world sketch {
    import blobs;
    export run: async func() -> result<u64>;
}
